1493. Longest Subarray of 1's After Deleting One Element
Solution: 


class Solution {
    public int longestSubarray(int[] nums) {
        int left = 0;
        int zeroCount = 0;
        int maxLen = 0;

        for (int right = 0; right < nums.length; right++) {
            // If we find a zero, increment zeroCount
            if (nums[right] == 0) {
                zeroCount++;
            }

            // If zeroCount > 1, shrink the window from left
            while (zeroCount > 1) {
                if (nums[left] == 0) {
                    zeroCount--;
                }
                left++;
            }

            // Update maxLen
            maxLen = Math.max(maxLen, right - left + 1);
        }

        // Since we must delete one element, subtract 1 from final length
        return maxLen - 1;
    }
}
