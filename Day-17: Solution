837. New 21 Game
Solution:
class Solution:
    def new21Game(self, n: int, k: int, maxPts: int) -> float:
        # Edge case: if k == 0 or n >= k + maxPts - 1
        if k == 0 or n >= k + maxPts - 1:
            return 1.0

        dp = [0.0] * (n + 1)
        dp[0] = 1.0

        window_sum = 1.0  # running sum of last maxPts dp values
        result = 0.0

        for i in range(1, n + 1):
            dp[i] = window_sum / maxPts

            # Only accumulate if i < k (still drawing cards)
            if i < k:
                window_sum += dp[i]
            else:
                # Otherwise, Alice has stopped, count it in final probability
                result += dp[i]

            # Remove the (i - maxPts) element from window if out of range
            if i - maxPts >= 0:
                window_sum -= dp[i - maxPts]

        return result
